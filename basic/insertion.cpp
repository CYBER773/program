#include<iostream>
using namespace std;
int main()
{
    int a, b;
    // // cin>>a>>b; we can use this that will take value at once
    // cout<<"Enter the value of a: ";
    // cin>>a;
    // cout<<"Enter the value of b: ";
    // cin>>b;
    // cout<<"Sum of a and b is "<<a+b;
    a =6, b=34;
    cout<<(a == b);
}